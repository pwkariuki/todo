## todo

A simple command-line tool for managing a list of "to-do" items. This tool will let you keep track of items left in a project or activity. The tool will save the list of items in a file using the `JSON` format.